{
  "name": "fisheye",
  "version": "1.0.0",
  "description": "project 6 for openclassrooms",
  "private": true,
  "main": "index.html",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "sass": "sass --watch ./assets/scss/main.scss:public/css/style.css --style compressed",
    "prefix": "postcss ./public/css/style.css --use autoprefixer -d ./public/css/"
  },
  "author": "AM",
  "license": "ISC",
  "browserslist": "last 4 versions",
  "dependencies": {},
  "devDependencies": {
    "eslint": "^8.4.0"
  }
}
